import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Dr. Serena Blake - Clinical Psychologist | Los Angeles Therapy',
  description: 'Licensed clinical psychologist in Los Angeles offering anxiety therapy, relationship counseling, and trauma recovery. Virtual and in-person sessions available.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
}